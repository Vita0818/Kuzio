# ARCHITECTURE

最近自查日期：2026-08-23

## 总体架构

Kuzio 当前尚无业务实现，也尚未选择产品架构。现阶段唯一成立的架构事实是：项目拥有独立 Git 边界和 Vitemis 项目文档基线。以下图示表达待确认顺序，不代表已经选定技术方案：

```text
用户确认产品目标
        |
        v
确认平台、约束与验收标准
        |
        v
记录 exact dependency 与官方 API 决策
        |
        v
设计模块、数据链路和安全边界
        |
        v
建立工程、实现与验证（当前均未开始）
```

## 主要链路

当前不存在已实现或已确认的核心数据流、业务链路或外部调用。后续每条链路必须记录起点、关键类型与模块、终点以及关键不变量，不能把计划当成现状。

## 数据模型

| 类型 | 职责 | 持久化方式 | 关键字段约束 |
|---|---|---|---|
| 尚未定义 | `UNKNOWN` | `UNKNOWN` | `UNKNOWN` |

## 同步 / 通信机制

当前没有已选择或已实现的同步、IPC、文件交换、消息总线或网络通信机制。

## 安全机制

当前没有产品级认证、配对、加密、签名、凭据存储或能力权限实现。后续设计必须在实现前明确：

- 信任边界、威胁面和最小权限要求。
- 凭据与敏感数据的安全存储位置。
- 外部输入、网络请求和持久化数据的校验要求。
- 必须 fail closed 的安全检查点。

在这些事实确认前，不得声称 Kuzio 已具备任何安全保证。

## 模式开关 / 内核切换

当前不适用。仓库没有模式枚举、新旧内核或迁移路径。

## 与文档/源码的关系

- 本文结论来自 2026-08-23 对仓库根目录、Git 状态和 Vitemis 模板规则的检查。
- 当前没有业务源码、工程配置、构建脚本或测试可供交叉验证。
- 后续源码与本文冲突时，以源码和实际配置为准，并立即修正文档。

## 外部依赖决策与禁止兜底

- 当前没有选择或引入任何产品外部依赖。
- 未来必须记录每项外部能力对应的 exact dependency、官方 API/扩展点、版本或固定 identity，以及许可证、provenance、安全、平台和分发约束。
- 有外部依赖提供同等能力时必须直接调用官方能力；架构中不得并存第一方重写、替代 adapter/shim/compatibility layer、parallel backend、preview backend、shadow implementation 或运行时 fallback。
- 本地边界只允许官方 API 必需的最薄生命周期、类型、权限、配置和 bundle 接线，不得复制依赖核心逻辑。
- 依赖无法接入时必须把能力标为 blocked 并明确失败；不得用 legacy、另一 provider/backend、mock、cache 或简化实现冒充完成。
- 安全 fail-closed 和明确要求的数据迁移必须与产品能力分开记录，不能演化成备用实现。
